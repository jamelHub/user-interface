
import styles from './Button.module.css'; // Importez le module CSS

const Button = () => {
    return (
        <button className={styles.btn}>
            Submit
        </button>
    );
};

export default Button;
